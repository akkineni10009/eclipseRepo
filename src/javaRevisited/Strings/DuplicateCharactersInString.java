package javaRevisited.Strings;

public class DuplicateCharactersInString {
  /*
   * Iterate through all the characters in the String and put them in a HashMap
   * Now, iterate through the keySet() of the map and find those keys whose value is more than 1
   * Print both the key and the value 
  */
}
